
import React from "react";
import { cn } from "@/lib/utils";

type StepIndicatorProps = {
  currentStep: string;
};

export function StepIndicator({ currentStep }: StepIndicatorProps) {
  // Componente vazio, removendo os elementos visuais como solicitado
  return null;
}
