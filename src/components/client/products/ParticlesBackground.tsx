
import { ParticlesBackgroundProps } from './particles/types';

export function ParticlesBackground({ style = "default" }: ParticlesBackgroundProps) {
  // Component now returns null (no particles)
  return null;
}
