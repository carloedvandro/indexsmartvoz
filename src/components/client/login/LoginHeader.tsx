
import React from 'react';

interface LoginHeaderProps {
  itemVariants?: any;
}

export function LoginHeader({ itemVariants }: LoginHeaderProps) {
  // Este componente não é mais usado, pois o logo foi movido para o cabeçalho da página
  return null;
}
