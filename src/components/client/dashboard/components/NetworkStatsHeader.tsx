import { CardHeader, CardTitle } from "@/components/ui/card";

export const NetworkStatsHeader = () => {
  return (
    <CardHeader className="pt-2">
      <CardTitle className="text-left font-bold">Estatísticas da Rede</CardTitle>
    </CardHeader>
  );
};